# Root compose env — place at /srv/kartoteka/.env
# Only Caddy reads this file (via env_file: .env in compose.yml).
# Do NOT add app secrets here — they would be visible to the Caddy container.

KARTOTEKA_DOMAIN=kartoteka.example.com
# KARTOTEKA_PROD_TAG=v0.4.1   # uncomment to pin prod to a specific version
